/**
 * Константы для типов графов
 * Используются для фильтрации доступных нод в зависимости от контекста
 */

/**
 * @typedef {('all'|'command'|'event')} GraphType
 */

const GRAPH_TYPES = {
    /** Нода доступна во всех типах графов */
    ALL: 'all',
    /** Нода доступна только в графах команд */
    COMMAND: 'command',
    /** Нода доступна только в графах событий */
    EVENT: 'event',
};

module.exports = {
    GRAPH_TYPES,
};
